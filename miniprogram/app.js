// F1 Armchair Pitwall — WeChat mini-program.
// The race engine (miniprogram/lib/) is the SAME pure-TS engine the web app runs,
// compiled to CommonJS (scripts/build-miniprogram.mjs). No mock data.
App({
  globalData: {},
});
